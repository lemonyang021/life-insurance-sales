spring:
   profiles:
      active: local
   application:
      name: salesbs-config
server:
   servlet:
      context-path: /salesbs-config
ebao:
   config:
      center:
         enabled: true
         url: http://configcenter-service.ebao
         profile: salesbs-config
---
spring:
   profiles: local
ebao:
   config:
      center:
         enabled: false
---
spring:
   profiles: sit
ebao:
   config:
      center:
         enabled: false