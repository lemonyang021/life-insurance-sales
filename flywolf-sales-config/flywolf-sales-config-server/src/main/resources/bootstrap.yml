spring:
   profiles:
      active: local
   application:
      name: sales-config

server:
   servlet:
      context-path: /sales-config
---
spring:
   profiles: local
ebao:
   config:
      center:
         enabled: false
---
spring:
   profiles: sit
ebao:
   config:
      center:
         enabled: false