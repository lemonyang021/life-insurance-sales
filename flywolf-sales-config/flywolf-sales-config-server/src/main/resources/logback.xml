<?xml version='1.0' encoding='UTF-8'?>
<!--日志配置-->
<configuration>
    <!--直接定义属性(全局变量)-->
    <!-- 定义日志存储位置变量，可以是相对路径也可以是绝对路径 -->
    <property name="testLogFile" value="logs/testLog"/>
    <property name="serviceLogFile" value="logs/serviceLog"/>
    <property name="controllerLogFile" value="logs/controllerLog"/>
    <property name="maxFileSize" value="3MB"/><!-- 单个日志文件大小 -->
    <property name="maxHistory" value="30"/><!--保留最近X天的日志-->
    <property name="totalSizeCap" value="1GB"/> <!--用来指定日志文件的上限大小，那么到了这个值，就会删除旧的日志-->
    <property name="pattern" value="%d [%thread] %-5level -[%file:%line]- %msg%n"/> <!--日志输出格式-->
    <property name="fileNamePattern" value=".%d{yyyy-MM-dd}.%i.log"/><!--归档日志文件后缀-->
    <property name="charset" value="UTF-8"/><!--日志字符集-->

    <!-- appender类似于一个日志格式化模板，描述了日志的格式、规则、输出形式等 -->

    <!--控制台日志定义模板-->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- %d代表日期，%thread代表线程名，%-5level表示级别从左显示 5 个字符宽度，
            %logger{50}表示 Logger 名字最长 50 个字符，[%file:%line]表示哪个类文件和第几行，
            %msg代表日志消息，%n代表换行-->
            <pattern>${pattern}</pattern>
            <charset>${charset}</charset>
        </encoder>
    </appender>

    <!--滚动文件日志定义模板-->
    <appender name="testFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${testLogFile}.log</file>
        <encoder>
            <!--日志输出格式-->
            <pattern>${pattern}</pattern>
            <charset>${charset}</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${testLogFile}${fileNamePattern}</fileNamePattern>
            <maxFileSize>${maxFileSize}</maxFileSize>
        </rollingPolicy>
    </appender>

    <!--服务日志定义模板-->
    <appender name="serviceFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${serviceLogFile}.log</file>
        <encoder>
            <!--日志输出格式-->
            <pattern>${pattern}</pattern>
            <charset>${charset}</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${serviceLogFile}${fileNamePattern}</fileNamePattern>
            <maxFileSize>${maxFileSize}</maxFileSize>
        </rollingPolicy>
    </appender>

    <!--控制器日志定义模板-->
    <appender name="controllerFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${controllerLogFile}.log</file>
        <encoder>
            <!--日志输出格式-->
            <pattern>${pattern}</pattern>
            <charset>${charset}</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${controllerLogFile}${fileNamePattern}</fileNamePattern>
            <maxFileSize>${maxFileSize}</maxFileSize>
            <maxHistory>${maxHistory}</maxHistory>
            <totalSizeCap>${totalSizeCap}</totalSizeCap>
        </rollingPolicy>
    </appender>


    <!--实例化日志输出-->
    <logger name="com.flywolf.li.sales.config.channel.controller" level="info" additivity="true">
        <!--可以有多个appender-ref，即将日志记录到不同的位置-->
        <!-- <appender-ref ref="STDOUT"/> -->
        <appender-ref ref="controllerFile"/>
    </logger>
    <logger name="com.flywolf.li.sales.config.channel.service" level="info" additivity="true">
        <!--可以有多个appender-ref，即将日志记录到不同的位置-->
        <!-- <appender-ref ref="STDOUT"/> -->
        <appender-ref ref="serviceFile"/>
    </logger>

    <!--基础的日志输出-->
    <root level="info">
        <appender-ref ref="testFile"/>
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>